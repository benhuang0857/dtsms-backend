# ==================== Server Configuration ====================
# TCP server port
TCP_PORT=8888

# ==================== MinIO Configuration ====================
# MinIO server address (host:port)
MINIO_HOST=minio:9000

# MinIO credentials
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=minio123

# MinIO bucket name for incoming files
MINIO_BUCKET=incoming

# ==================== Security Configuration ====================
# HMAC secret for authentication (change this in production!)
HMAC_SECRET=supersecretkey

# ==================== TLS/SSL Configuration ====================
# Enable TLS (true/false)
TLS_ENABLE=true

# TLS certificate paths (used when TLS_ENABLE=true)
TLS_CERT=/certs/server.crt
TLS_KEY=/certs/server.key
TLS_CA=/certs/ca.crt
